apiVersion: v1
kind: Service
metadata:
  name: {{ include "multicloud-demo-app.fullname" . }}
  labels:
    {{- include "multicloud-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "multicloud-demo-app.selectorLabels" . | nindent 4 }}
